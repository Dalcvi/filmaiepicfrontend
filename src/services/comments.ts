// export const postComment = async (postId: number, text: string, name: string) => {
//       const posts = await axios.post(`https://localhost:5001/FilmaiOut/Posts/post/${postId}/comment`, {
//           name, text
//       });
// }
